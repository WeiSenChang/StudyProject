void BubbleSort(std::vector<int>& nums)//使用vector容器代替数组
{
	for (int i = 0; i < nums.size() - 1; i++)
	{
		for (int j = 0; j < nums.size() - i - 1; j++)
		{
			if (nums[j] > nums[j + 1])
			{
				std::swap(nums[j], nums[j + 1]);//使用标准库的交换函数
			}
		}
	}
}
